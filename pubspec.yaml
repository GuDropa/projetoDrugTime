name: cli
description: A sample command-line application.
version: 1.0.0
# homepage: https://www.example.com

environment:
    sdk: ">=2.14.4 <3.0.0"

dependencies:
    path: ^1.8.0
    intl:

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0
dependencies: 
  image_picker: ^0.8.4+3

